export * from "@pidesk/shell-model";

// Web previously contained a copy of the feed implementation. We now
// re-export the shared package's feed utilities so web code imports the
// canonical implementation.
