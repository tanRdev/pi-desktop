{
  "name": "@pi-desktop/agent-host",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc --project tsconfig.build.json --declaration false",
    "dev": "tsc --project tsconfig.build.json --declaration false --watch --preserveWatchOutput",
    "typecheck": "tsc --project tsconfig.json --noEmit"
  },
  "dependencies": {
    "@earendil-works/pi-coding-agent": "0.79.0",
    "@pi-desktop/shared": "workspace:*",
    "effect": "^3.21.0"
  }
}
