This project uses bun - not npm or pnpm. Prefix the run dev command with `openlogs` when you run the server.

Use vitest for all unit tests.
Write unit tests regularly and update them when needed.
Never write brittle tests.

Use the `Effect` library for typescript (effect-ts)